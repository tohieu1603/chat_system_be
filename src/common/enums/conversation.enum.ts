export enum ConversationType {
  AI_COLLECT = 'AI_COLLECT',
  SUPPORT = 'SUPPORT',
  CLARIFY = 'CLARIFY',
}

export enum ConversationStatus {
  ACTIVE = 'ACTIVE',
  PAUSED = 'PAUSED',
  COMPLETED = 'COMPLETED',
}
